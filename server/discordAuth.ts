import express from "express";
import passport from "passport";

// Import the strategy and types from @oauth-everything/passport-discord
import {
  Strategy,
  Profile,
  VerifyCallback /*, Scope*/,
} from "@oauth-everything/passport-discord";

// connection

// authenticate

// user

// redirect success
// redirect fail
